import Typography from "@mui/material/Typography";
import React from "react";

type Props = {};

const Forbidden = (props: Props) => {
    // TODO
    return <Typography variant="h4">Forbidden</Typography>;
};

export default Forbidden;
